'use client';

/* =====================================================
   CLIENTS TABLE — DOMAIN CLIENTS
   Reutiliza tabla admin sin romper dominio
===================================================== */

import AdminClientsTable from '@/app/admin/AdminClientsTable';

export default function ClientsTable() {
  return <AdminClientsTable />;
}
